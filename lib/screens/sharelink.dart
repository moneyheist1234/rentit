import 'package:flutter/material.dart';

class Sharelink extends StatelessWidget {
  const Sharelink({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('share link goes here'),
      ),
    );
  }
}
