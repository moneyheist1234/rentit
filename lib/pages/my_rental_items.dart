import 'package:flutter/material.dart';

class MyRentalItems extends StatelessWidget {
  const MyRentalItems({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('my rental items page opened from dashbar'),
    );
  }
}
