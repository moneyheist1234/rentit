import 'package:flutter/material.dart';

class Ownership extends StatelessWidget {
  const Ownership({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text(''
          'ownership page from dashbar'),
    );
  }
}
